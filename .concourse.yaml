---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: clojure
    tag: alpine

inputs:
- name: git-repo

run:
  path: sh
  args:
  - -exc
  - |
    cd git-repo
    lein cloverage
